<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true" use-expressions="true">
        <intercept-url pattern="/seedstartermng" access="hasRole('Administrateur')"/>
        <intercept-url pattern="/creerBonLivraison" access="hasRole('Administrateur')"/>



        <!-- access denied page -->
        <access-denied-handler error-page="/login-error"/>
        <form-login
                login-page="/login"

                authentication-success-forward-url="/seedstartermng"
                username-parameter="username"
                password-parameter="password"/>
        <!-- enable csrf protection -->
        <csrf/>
    </http>

    <authentication-manager>
        <authentication-provider user-service-ref="myUserDetailsService">
        </authentication-provider>
    </authentication-manager>

</beans:beans>